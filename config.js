/* =============================================================
   Veblix — конфігурація сайту
   =============================================================

   ── Telegram-бот для заявок (одноразово, ~3 хв) ──────────────

   1. У Telegram відкрий @BotFather → /newbot. Дай ім'я
      (напр. "Veblix Orders") і логін (напр. veblix_orders_bot).
      BotFather поверне токен виду 1234567890:AAH-xxx… — скопіюй.

   2. Знайди свого бота за логіном і натисни Start (один раз),
      щоб він міг писати тобі в особисті.

   3. Відкрий @userinfobot → Start — він поверне твій числовий
      ID (напр. 123456789). Це і є TG_CHAT_ID.

   4. Встав обидва значення нижче та збережи файл. Поки вони
      порожні — форма працює в demo-режимі (показує успіх,
      нічого не надсилає).

   ── Курс долара ─────────────────────────────────────────────
   UAH_RATE — за скільки гривень рахувати $1 (для показу ₴-цін
   поруч із $). Онови за потреби.

   УВАГА: цей файл лежить у відкритому коді сайту, тож токен
   видно будь-якому відвідувачу. Це нормально для маленького
   "write-only" бота, що пише лише тобі. Якщо почнеться спам —
   видали бота в @BotFather і створи нового, оновивши токен.
   ============================================================= */

window.VEBLIX_CONFIG = {
  TG_BOT_TOKEN: '',
  TG_CHAT_ID:   '',
  UAH_RATE:     41.5,        // 1 USD ≈ … ₴
  PAY_REQUISITES: 'veblix.studio (PayPal / картка — уточнюй у відповіді)',

  // ── Meta (Facebook) Pixel для реклами ───────────────────────
  // Створи піксель у Meta Events Manager → встав сюди числовий ID
  // (напр. '1234567890123456'). Поки порожньо — піксель не вантажиться
  // (сайт працює як завжди). Події: PageView, Lead (форма), Contact (Telegram).
  META_PIXEL_ID: '',
};
